# 02 反馈闭环

用户反馈只有进入评估集，才会真正改善系统。

## 流程

```text
用户点踩
  ↓
保存 message_id 和 trace_id
  ↓
人工标注失败原因
  ↓
转成 eval case
  ↓
修复 prompt / 数据 / 检索
  ↓
CI 回归
```

## 失败类型

- 检索未命中
- 检索命中但排序低
- 上下文有但模型答错
- 引用错误
- 权限错误
- 信息过期

## 练习

设计一张 `feedback_cases` 表，支持把用户反馈转换为评估样本。
