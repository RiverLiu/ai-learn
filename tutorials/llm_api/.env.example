# 复制本文件为项目根目录的 .env 并填入真实配置（.env 已被 .gitignore 忽略，不会提交）
#   cp tutorials/llm_api/.env.example .env
OPENAI_API_KEY=sk-...

# 使用 OpenAI 兼容服务时，再配置服务地址与模型名。注意：
# OPENAI_BASE_URL 只到 /v1 为止，不要带 /chat/completions 等路径后缀，
# 否则 SDK 拼出来的最终地址会重复一段路径，导致 404。
#
# Kimi（Moonshot）示例：
# OPENAI_BASE_URL=https://api.moonshot.cn/v1
# MODEL_NAME=kimi-k2.6
#
# DeepSeek 示例：
# OPENAI_BASE_URL=https://api.deepseek.com/v1
# MODEL_NAME=deepseek-chat
#
# OpenAI 官方：不需要 OPENAI_BASE_URL，配置 MODEL_NAME 即可（也可省略，默认 gpt-4o-mini）
# MODEL_NAME=gpt-4o-mini
