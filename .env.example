# 全课程统一配置模板：复制为项目根目录的 .env 并填入真实值
# （.env 已被 .gitignore 忽略，不会提交；各章代码 load_dotenv() 会向上查找到它）

# —— 聊天模型（必需）——
OPENAI_API_KEY=sk-...
# 使用 OpenAI 兼容服务时设置；注意只到 /v1 为止，不要带 /chat/completions 等路径后缀
# OPENAI_BASE_URL=https://api.kimi.com/coding/v1
# MODEL_NAME=kimi-for-coding

# —— 向量模型（仅 RAG / memory / evaluation 等章节需要）——
# EMBEDDING_MODEL=text-embedding-3-small

# 常见兼容服务示例：
# 阿里百炼（聊天+向量同端点）：OPENAI_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
#   MODEL_NAME=qwen-plus  EMBEDDING_MODEL=text-embedding-v4
# 硅基流动（聊天+向量同端点）：OPENAI_BASE_URL=https://api.siliconflow.cn/v1
#   MODEL_NAME=Qwen/Qwen3-32B  EMBEDDING_MODEL=BAAI/bge-m3
# 本地 Ollama：OPENAI_BASE_URL=http://localhost:11434/v1  OPENAI_API_KEY=ollama

# 注意：聊天与向量若分属两家服务（如 Kimi 聊天 + 百炼向量），
# 单一 OPENAI_BASE_URL 无法共存——此时把向量配置放到 tutorials/07_rag/01_embeddings/.env
# （该章会优先读取本章 .env），或统一换用一家同时提供两类模型的服务。

# —— LangSmith 追踪（仅 langchain/06_langsmith 需要）——
# LANGSMITH_TRACING=true
# LANGSMITH_API_KEY=ls__...
# LANGSMITH_PROJECT=ai-guide
